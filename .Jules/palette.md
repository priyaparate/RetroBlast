## 2024-10-24 - Accessibility Patterns
**Learning:** The application frequently uses inputs with placeholders but no visible labels, and icon-only links for social media.
**Action:** Ensure all inputs have `aria-label` matching their placeholder, and icon-only links have descriptive `aria-label` attributes.
