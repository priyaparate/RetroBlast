## 2026-01-31 - Form Inputs Missing Labels
**Learning:** Form inputs in the application primarily rely on placeholders without visible labels, requiring `aria-label` attributes for accessibility.
**Action:** Always check for and add `aria-label` to input fields where visible labels are absent to ensure screen reader support.
